{
  "name": "todolist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel src -d dist --presets=@babel/preset-env -ws",
    "build": "rm -rf dist && babel src -d dist --presets=@babel/preset-env ", "serve": "http-server",
    "wServe": "concurrently \"npm run watch\" \"npm run serve\""
    },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.13.16",
    "@babel/core": "^7.14.0",
    "@babel/preset-env": "^7.14.0",
    "concurrently": "^6.0.2",
    "http-server": "^0.12.3"
  }
}
